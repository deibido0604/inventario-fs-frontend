// import { Breadcrumb} from 'antd';
// import { Link, useLocation } from 'react-router-dom';

export const BreadCrumbComponent = () => {
  // const location = useLocation();
  //  const pathname = location.pathname.replace("/main",'');
  // let currentLink = [];
  // const crumbs = pathname.split('/')
  //   .filter((crumb) => crumb !== '')
  //   .map((crumb) => {
  //     currentLink.push(`/main/${crumb}`);
  //     return {
  //       title: <Link to={currentLink.join('')}>{ crumb  } </Link>
  //             }
  //   })

  return (
    <br />
    // <Breadcrumb
    //         style={{
    //           margin: '16px 0',
    //   }}
    //   items={crumbs}
    //       >

    //       </Breadcrumb>
  );
};
